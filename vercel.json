{
    "buildCommand": "cd Saude/frontend && npm install && npm run build",
    "outputDirectory": "Saude/frontend/dist",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}